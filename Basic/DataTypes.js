"use strict"; // treat all js jc code as newer version

//alert(3 + 3) // ew are using the node js not browser

console.log(3+3); 

//code readability that shoukd high.

/**
 *   Datatype :-
 * 
 * number 
 * bigint 
 * string
 * boolean
 * null :- standalone value
 * undefined :-  valus not assigned.
 * symbol 
 * objects
 *
 */
console.log(typeof(null))
console.log( typeof(nudefined))